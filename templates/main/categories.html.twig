{% extends 'base.html.twig' %}

{% block title %} Categories {% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1> Categories ✅</h1>

    {% for category in categories %}
        <h2> <a href="{{ path('category', {'categorySlug' : category.slug}) }}"> {{ category.title }} </a> </h2>
        <ul>
            {% for secondOrderCategory in category.children %}
                <h3 style="margin-left:20px;"> <a href="{{ path('category', {'categorySlug' : secondOrderCategory.slug}) }}"> {{ secondOrderCategory.title }}</a> </h3>
                    {#{% for products in  secondOrderCategory.products %}#}
                        {#<li style="margin-left: 50px"> <a href="{{ path('product', {'categorySlug' : category.slug, 'productSlug' : product.slug}) }}"> {{ products.title }} </a> </li>#}
                    {#{% endfor %}#}
            {% endfor %}
        </ul>
    {% endfor %}
</div>
{% endblock %}
